/-
Development file of 2026-09-09, twenty second run: the general half of the splitting of the
driving data at the first jump.

Its contents have moved into `TauCeti/MartingaleProblems/Suggested.lean`, where they are
type-checked in place against Mathlib v4.33.1 and checked with `#print axioms`:
`map_prodMk_of_map_eq_dirac`, `natCons`, `measurable_natCons`, `infinitePi_map_natCons` and
`infinitePi_map_split`.

Nothing is left here on purpose: a scratch copy of a declaration that also lives in
`Suggested.lean` would be a second, unchecked source of truth.
-/
