/-
Development file of 2026-09-09, twenty first run: the shift at the first jump.

Its contents have moved into `TauCeti/MartingaleProblems/Suggested.lean`, where they are
type-checked in place against Mathlib v4.33.1 and checked with `#print axioms`:
`jumpShift`, `jumpTime_jumpShift`, `stepIndex_shift`, `jumpProcess_jumpShift`,
`waitingMeasure_map_shift`, `chainFam`, `shiftIic`, `piSingleton_apply_const`,
`partialTraj_succ_map_shiftIic`, `partialTraj_map_shiftIic`, `ext_of_map_frestrictLe`,
`traj_map_shift` and `chainKernel_map_shift`.

Nothing is left here on purpose: a scratch copy of a definition that also lives in
`Suggested.lean` would be a second, unchecked source of truth.
-/
